readingBuddy.service('RTC', function(){

	// var BinaryClient = require('binaryjs-client');
	// var client = new BinaryClient('localhost:8000');

	var socket = io.connect('localhost:8000');
	var stream = ss.createStream();
});